<!-- test2.ejs -->

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update Custom Command Settings</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            margin: 20px;
            padding: 20px;
            text-align: center;
            color: aliceblue;
            font-family: "Hind Vadodara", -apple-system, BlinkMacSystemFont, Segoe UI, Helvetica Neue, Arial, sans-serif;
            background: linear-gradient(to right, #1c1c1d, #182848);
        }

        h1 {
            color: #3498db;
        }

        form {
            max-width: 400px;
            margin: 20px auto;
            background-color: #2c3e50;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        label {
            display: block;
            margin-bottom: 8px;
            color: #ecf0f1;
        }

        input {
            width: 100%;
            padding: 8px;
            margin-bottom: 10px; /* Increase margin-bottom for better separation */
            border: 1px solid #3498db;
            border-radius: 4px;
            background-color: #34495e;
            color: #ecf0f1;
        }

        sub.hint {
            font-size: 10px;
            color: #ecf0f1; /* Set the hint color to match the label color */
            display: block; /* Ensures each hint is on a new line */
            margin-top: -8px; /* Adjust margin-top to bring hints closer to the input */
        }

        button {
            background-color: #3498db;
            color: #ecf0f1;
            padding: 10px 15px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
        }

        button:hover {
            background-color: #2980b9;
        }

        p {
            color: #2ecc71;
            margin-top: 10px;
        }
    </style>
</head>
<body>
    <h1>Update Custom Command Settings</h1>
    
    <form action="/dashboard/<%= guildID %>/update_command" method="post">
        <label for="trigger">Command Trigger:</label>
        <input type="text" id="trigger" name="trigger" required placeholder="Example: What is going on">
        <sub class="hint">Hint: This is the exact message which triggers the command</sub>

        <label for="content">Message Content:</label>
        <input type="text" id="content" name="content" required placeholder="Result: Not much... just hotsuop chilling here...">
        <sub class="hint">Hint: This is the content of the message that is sent</sub>

        <button type="submit">Update Custom Command Settings</button>
    </form>

    <% if (message) { %>
        <p><%= message %></p>
    <% } %>
</body>
</html>
